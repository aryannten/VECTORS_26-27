{
  "name": "vectors-26-27",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan.bold,green.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run dev",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "test": "npm test --prefix server"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "ogl": "^1.0.11"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "sharp": "^0.35.4"
  },
  "overrides": {
    "jose": "^4.15.9"
  },
  "engines": {
    "node": "22.x"
  }
}
